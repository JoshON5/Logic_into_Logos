const {Triangle, Square, Circle} = require('./shapes')
class ShapeRender {
    renderData() {
        if(Triangle == data.shape) {
            return `${render()}`
        }
        if(Square == data.shape)  {
            return `${render()}`
        }
        if (Circle == data.shape) {
            return `${render()}`
        }
    return this.renderData()
    }
}

module.exports = ShapeRender